# Step1
- 与えられたリスト nums をソートして、先頭から同じ値が何回連続して出現するかをカウントしていけば、各要素の出現回数が求まる。その後、出現回数が多い順に上位 k 個 を取り出せば解けそうでる。
- ただし、各要素の出現回数をどのように管理すればよいか分からず10分経過したため、解答を確認したところ、 dict を用いてカウントする方法を学んだ。
- その際、昔講義でハッシュテーブルを実装したことをふと思い出したので、 dict の実装についても調べてみたいと感じた。 dict の内部実装ではハッシュテーブルを利用しているため。

# Step2
- `num_frequency[num] += 1` の上行に空行を入れた。その方が読みやすいと感じたため。

# Step3
- 10分以内に3回連続でエラーを出さずに書いてアクセプトされた。