## Step1

まず、与えられた文字列 s の各文字の出現回数を辞書で管理する。次に、文字列を先頭から順に見ていき、出現回数が1回である最初の文字のインデックスを返すように実装した。

### 計算量

**時間計算量**
- O(N)
- N は文字列 s の長さ

**空間計算量**
- O(1)
- 辞書のサイズは最大で26のため（英子文字26種類より）

## Step2

Python 標準ライブラリである `collections.Counter` を使い、各文字のカウント処理を1行で書いた。

## Step3

10分以内に3回連続でエラーを出さずに書いてアクセプトされた。